import { DefaultTheme } from 'styled-components';
import colors from './colors';

export default {
  header: {
    navigation: {
      fontcolor: {
        default: colors.text,
        active: colors.purpleDark
      }
    }
  }
} as DefaultTheme;
